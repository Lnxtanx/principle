/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['school-management-uploads1.s3.eu-north-1.amazonaws.com'],
  },
};

export default nextConfig;
